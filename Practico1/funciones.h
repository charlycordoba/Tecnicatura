/** \brief Ingresa Un numero
 *
 * \param Pide un numero
 * \return Un numero sino sale y pide que reingrese el numero.
 *
 */


int numero (int num);


/** \brief Calcula la suma de 2 numeros.
 *
 * \param recibe un valor de x y un valor de y.
 * \return decuelve el resultado de la suma.
 */

float Suma(float x, float y);


/** \brief  calcula la resta de 2 numeros.
 *
 * \param Recibe un valor de x y otro de y para realizar la operacion.
 * \return Devuelve el resultado de la resta.
 */
float Resta(float x, float y);


/** \brief Calcula la division entre dos numeros
*
* \param Recibe un valor de x y otro de y para realizar la operacion.
* \return Devuelve el resultado de la operacion.
*/
float Division(float x,float y);


/** \brief Calcula la multiplicacion entre dos numeros.
 *
 * \param Recibe un valor para x y otro para Y.
 * \return devuelve el resultado de una operacion.
 */

 float Multiplicacion (float x, float y);


 /** \brief Calcula la recursividad de un numero.
  *
  * \param Recibe el valor de un numero y calcula el factorial del mismo.
  * \return Devuelve un el resultado de la operacion.
  */
  float Factorial(int x);






